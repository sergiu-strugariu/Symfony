<div class="item">
    <div>
        <img src="{{ asset(app_page_widget_path ~ widget.fileName, 'cloudflare') }}" alt="{{ widget.name }}"/>
        <h3>{{ widget.name }}</h3>
    </div>
    {{ widget.description|raw }}
</div>