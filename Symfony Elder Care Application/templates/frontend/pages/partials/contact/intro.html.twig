<div class="intro" data-image="{{ asset(app_page_section_path ~ section.fileName, 'cloudflare') }}">
    <div class="inner" >
        <h1>{{ section.name }}</h1>
        {{ section.description|raw }}
    </div>
</div>
