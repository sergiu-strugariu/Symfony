<div class="benefits">
    <div class="inner">
        <div class="content">
            <!-- Start: Page widget  -->
            {% for widget in section.pageWidgets %}
                {% set translation = widget.translation(locale) %}
                {% include 'frontend/pages/partials/benefits/widgets/' ~ widget.template ignore missing %}
            {% endfor %}
            <!-- End: Page widget  -->
        </div>
    </div>
</div>