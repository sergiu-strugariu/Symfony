<div class="info-card" {% if widget.fileName %} data-image="{{ asset(app_page_widget_path ~ widget.fileName, 'cloudflare') }}" {% endif %}>
    <div class="info-card-inner">
        {% if translation.description %}
            {{ translation.description|sanitize_html('app.editor') }}
        {% endif %}
    </div>
</div>