<div class="timeline__item">
    {% if translation.title %}
        <p class="year">{{ translation.title }}</p>
    {% endif %}
    <span class="dot"></span>
    <div>
        {% if translation.description %}
            {{ translation.description|sanitize_html('app.editor') }}
        {% endif %}
    </div>
</div>