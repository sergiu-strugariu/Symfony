{% if widget.fileName %}
    <div class="intro-section" data-image="{{ asset('assets/files/page/pagewidget/' ~ widget.fileName) }}">
        <div class="inner">
            {% if translation.title %}
                <h1>{{ translation.title }}</h1>
            {% endif %}
        </div>
    </div>
{% endif %}