
{% for widget in section.pageWidgets %}
    {% set translation = widget.translation(locale) %}
    <div class="common-intro" data-image="{{ asset('assets/files/page/pagewidget/' ~ widget.fileName) }}">
        <div class="inner">
            <h1>{{ translation.title }}</h1>
        </div>
    </div>
{% endfor %}

{% include 'frontend/default/partials/courses-calendar/courses-list.html.twig' %}

